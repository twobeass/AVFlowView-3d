// Placeholder for SchemaValidator implementation (Phase 2).
// The real implementation will wrap Ajv and expose:
//   validateGraph(json: unknown): { valid: boolean; errors?: AjvError[] }

export class SchemaValidator {
  // eslint-disable-next-line class-methods-use-this
  validateGraph() {
    throw new Error('SchemaValidator.validateGraph is not implemented yet.');
  }
}
